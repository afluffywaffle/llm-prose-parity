# secrets & local config — NEVER commit
config.json
*.key
.openrouter*
.env

# run outputs (regenerated locally; keep the repo clean)
task/
runs/
__pycache__/
*.pyc
.DS_Store

# local run-log telemetry (never committed)
run_log.jsonl
